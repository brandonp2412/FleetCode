let index = 0;

index = index + 1;

// Is the same as 

index += 1;

// Is the same as 

index++;

let daysInWeek = 0;
for (let day = 0; day < 7; day++) {
    daysInWeek++;
}

// What is this going to output?
console.log('There are ' + daysInWeek + ' days in a week.');